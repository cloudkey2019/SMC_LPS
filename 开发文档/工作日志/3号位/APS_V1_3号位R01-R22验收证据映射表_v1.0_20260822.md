# APS V1 3号位 R01～R22 验收证据映射表

> 版本：v1.0
> 日期：2026-08-22
> 编制：3号位
> 依据：《APS_V1_3号位规则参数与运行生命周期开发实施包_v1.0_20260814》§25（R01~R22 最低验收场景）
>        及二轮复审报告《APS_V1_3号位代码第二轮复审核报告_Commit锁定版_v1.2_20260821》P1-02（"下一轮至少必须补/修…"）
> 执行证据：本表测试均于 2026-08-22 在仓库内实际执行 `dotnet test` 验证（非仅源码审查）。

---

## 一、测试执行总览（2026-08-22 实测）

```
dotnet build LPS.APS.sln   → 0 错误
dotnet test LPS.APS.Tests  → 总计 97 / 通过 90 / 跳过 6 / 失败 1
```

- **6 跳过** = 治理链路集成测试统一 `[SkippableFact]`，依赖 APS_Auth 库与 `ScheduleRun.ExpectedDomainKeysJson` 列；环境补齐后自动转绿无需改测试（见《APS_V1_3号位P0整改复核提报_v1.0_20260821》§六）
- **1 失败** = 既有 2 号位 `FiniteCapacitySolver` DI 问题（`完整排程流程集成测试`：`Unable to resolve service for type 'LPS.APS.Scheduling.Solvers.FiniteCapacitySolver'`），非 3 号位范围（红线：不擅改 2 号位），见 §四

---

## 二、R01～R22 逐项映射与状态

| 编号 | 验收场景（实施包） | 测试文件 | 关键测试方法 | 状态 |
|---|---|---|---|---|
| R01 | 发布 RuleSetVersion，历史版本不可被覆盖 | `RuleSetVersionPublishTests` | `Publish_DraftVersion_StatusBecomesPublished` / `Publish_AlreadyPublishedVersion_ThrowsInvalidOperation` / `Publish_NonexistentVersion_ThrowsInvalidOperation` | ✅ 绿 |
| R02 | 发布 ParameterSetVersion，新 Run 可引用、旧 Run 不变 | `ParameterSetVersionPublishTests` | `Publish_NewV2_OldV1RecordUnchanged` / `Publish_DraftVersion_StatusBecomesPublished` / `Publish_AlreadyPublishedVersion_ThrowsInvalidOperation` | ✅ 绿 |
| R03 | Run 启动后发布新规则，当前 Run 仍用旧冻结版本 | `FrozenStrategySnapshotProviderTests` | `同一版本二次调用_命中缓存不重复查库` / `不同版本_互不污染各走仓储` / `缓存命中_反序列化重建独立实例_内容值相等` | 🟡 部分（Snapshot 按 VersionId 重放四块真实；Solver/Candidate 两块受 P0-02 阻塞，见 §三） |
| R04 | Priority Segment 第一命中 | `DemandPriorityMatcherTests` | `R04_第一命中即止_命中第一个Segment后不再匹配后续` | ✅ 绿 |
| R05 | 同 Segment 多 Sort 字段顺序稳定 | `DemandPriorityMatcherTests` | `R05_多Sort稳定排序_依次应用排序字段` | ✅ 绿 |
| R06 | 不同 DemandType 同层竞争按 Segment 交错，不用全局 Score | `DemandPriorityMatcherTests` | `R06_不同DemandType交错_按Segment分组后组内排序` | ✅ 绿 |
| R07 | Demand Protection 触发，2号位能读取触发配置 | `StageDTests` | `R07_Protection触发配置_可读取` | ✅ 绿 |
| R08 | PI 默认排序（Issue/Create Time） | `StageDTests` | `R08_PI默认排序_可读取` / `R08_PI排序枚举_覆盖Issue与CreatedAt两种规则` | ✅ 绿 |
| R09 | Warehouse Availability 规则可解析 | `StageDTests` | `R09_WarehouseAvailability规则_可解析` / `R09_WarehouseAvailability_契约JSON形状_可被下游反序列化` | ✅ 绿 |
| R10 | Manual ETA > ERP ETA > DefaultLT 参数链可表达 | `StageDTests` + `EtaInvariantTests` | `R10_ETA链_优先级固定可表达` + `Resolve_Manual存在_无论ERP与DefaultLT如何_一律以Manual为准`（等 6 项 ETA 不变量） | ✅ 绿 |
| R11 | DefaultLT 逾期 Margin 可配置 | `StageDTests` | `R11_逾期Margin_可配置` | ✅ 绿 |
| R12 | Arrival Offset 按 Warehouse 级生效 | `StageDTests` | `R12_ArrivalOffset_按Warehouse级生效` | ✅ 绿 |
| R13 | Planning Yield 2号位可取正确版本值 | `StageDTests` + `FrozenStrategySnapshotProviderTests` | `R13_PlanningYield_可取正确版本值`（Procurement 块含 PlanningYields） | ✅ 绿 |
| R14 | MIXED Solver Strategy 1号位能读取 | `FrozenStrategySnapshotProviderTests`（待补具体值断言） | — | ❌ 阻塞（P0-02 待 0 号位 DDL 裁决；Provider 仍 `new SolverStrategyBlock()` 空对象） |
| R15 | On-time Target 取值正确 | 同上（SolverStrategy 内） | — | ❌ 阻塞（同 R14） |
| R16 | Candidate 60/90/180 Snapshot 中正确 | 同上（CandidateGuardrail 内） | — | ❌ 阻塞（同 R14） |
| R17 | MaxImpactedOrders 仅 Warning 语义 | 同上（CandidateGuardrail 内） | — | ❌ 阻塞（同 R14） |
| R18 | FULL ExpectedDomainKeysJson 多 Domain 合法 | `RunLifecycleServiceTests` | `Validate_FullSchedule_两Domain_通过` / `Validate_FullSchedule_重复DomainKey_抛异常`（P1-01 新增） | ✅ 绿 |
| R19 | Candidate ExpectedDomainKeysJson 多于 1 个拒绝 | `RunLifecycleServiceTests` | `Validate_Reschedule_两Domain_抛异常` / `Validate_Reschedule_恰一Domain_通过` / `Validate_数组含空DomainKey_抛异常` | ✅ 绿 |
| R20 | FAILED Run 人工恢复新建 Run | `RunLifecycleServiceTests` | `Recover_合法_新建RUNNING继承基线_旧记录不动` / `Recover_旧Run非FAILED_抛异常` / `Recover_继承基线不合法_抛异常且不新建` | ✅ 绿 |
| R21 | Candidate 确认 Actor/Time/Version 可追溯 | `RunLifecycleServiceTests` | `Confirm_合法_仅记审计不写Activated` / `Confirm_同域已有ACTIVE_仍可正常确认` / `Activate_未确认_抛异常` / `Activate_合法_已确认且来源可激活_状态转ACTIVE` | ✅ 绿（本轮改写为 P0-04/05/06 语义） |
| R22 | OA 不可用不阻断最小人工确认 | `RunLifecycleServiceTests` | 确认=仅审计落点，无 OA 依赖（`Confirm_*` 系列） | ✅ 绿 |

**合计：18/22 全绿（R01~R13、R18~R22）；R14~R17 因 P0-02 阻塞待收口。**

---

## 三、二轮复审 P1-02 要求逐项核对（本轮新增/改写证据）

二轮复审报告 §5 P1-02 点名"下一轮至少必须补/修"的项：

| 要求 | 本轮动作 | 证据 |
|---|---|---|
| 完整 Snapshot 具体值 | 已有四块具体值断言；Solver/Candidate 待 P0-02 | `FrozenStrategySnapshotProviderTests`（装载/缓存/失败） |
| Run 中规则不漂移 | 已有版本隔离 + 缓存不污染断言 | `FrozenStrategySnapshotProviderTests`（同一版本二次调用/不同版本互不污染） |
| PlanningYield | 已有 | `StageDTests.R13` |
| SolverStrategy / CandidateGuardrail | 阻塞（P0-02，见《P0整改复核提报》§五） | 待 0 号位 DDL 方案裁决 |
| CTP/Impact 永不激活 | ✅ 本轮新增 | `Activate_来源Run为INSERT_ORDER_WHATIF_抛异常` / `Activate_SourceScheduleRunId为空_抛异常` |
| 确认后才激活 | ✅ 本轮新增 | `Activate_未确认_抛异常` |
| Base ACTIVE 原子替换 | ✅ 本轮改写 | `Activate_同域已有ACTIVE_原子替换` / `Confirm_同域已有ACTIVE_仍可正常确认` |
| FULL 重复 Domain 拒绝 | ✅ 本轮新增 | `Validate_FullSchedule_重复DomainKey_抛异常` |
| FAILED 新 Run | 已有 | `Recover_*` 4 项 |

## 四、未关闭项与跨号位依赖

1. **R14~R17（SolverStrategy / CandidateGuardrail / On-time / MaxImpactedOrders）**：依赖《P0-01 六块Snapshot持久化来源映射表 + 最小DDL差异》0/2/3 号位技术确认（DDL 方案 A/B/C）。裁决后立即收口：为 Solver/Candidate 建立真实版本来源并补具体值断言。
2. **1 个既有失败**（`完整排程流程集成测试`，2 号位 `FiniteCapacitySolver` DI）：归属 2 号位，见《P0整改复核提报》§六.3，建议 `AddSchedulingServices()` 补 `services.AddSingleton<FiniteCapacitySolver>()` 或 `SchedulingOrchestrator` 改依赖接口。
3. **6 个跳过（环境）**：APS_Auth 库 + `ExpectedDomainKeysJson` 列迁移后自动转绿（2 号位协同）。

---

## 附：本轮 3 号位代码变更清单（相对 Commit 基线）

| 文件 | 变更 |
|---|---|
| `LPS.APS.Application\Services\RunLifecycleService.cs` | P0-03/04/05/06 + P1-01 主整改 |
| `LPS.APS.Core\Interfaces\IPlanVersionRepository.cs` | 移除 `GetActiveByDomainKeyAsync`、新增 `ReplaceActiveAsync` |
| `LPS.APS.Engine\Repositories\Governance\PlanVersionRepository.cs` | 实现原子替换 `ReplaceActiveAsync` |
| `LPS.APS.Tests\Unit\RunLifecycleServiceTests.cs` | 改写 4 错误测试 + 新增 5 测试（27 项全绿） |
| `LPS.APS.Tests\Integration\RunLifecycleServiceIntegrationTests.cs` | 确认/激活断言对齐 P0-05 |
| `开发文档\契约文档\05_3号位和1号位对外契约.md` | §6.11 契约收紧（红线 #5） |
| `README.md` | P1-03 最小去旧化（BusinessRules Deprecated 说明） |
