# APS V1 3号位 R01～R22 验收证据映射表

> 版本：v2.0（裁决后收口版）
> 日期：2026-08-22
> 编制：3号位
> 依据：《APS_V1_3号位规则参数与运行生命周期开发实施包_v1.0_20260814》§25（R01~R22 最低验收场景）
>        及二轮复审报告《APS_V1_3号位代码第二轮复审核报告_Commit锁定版_v1.2_20260821》P1-02
>        + 0号位《第三轮正式复审报告_a5741a7_Commit冻结版(1).md》§12.3（**批准方案 A**，2026-08-22）
> 变更：v1.0 基线（第三轮提报）→ v2.0 收口 P0-01b/P0-02a/P0-02b（#16~#19），R03/R14~R17 由 🟡/❌ 转 ✅
> 执行证据：本表测试均于 2026-08-22 在仓库内实际执行 `dotnet test` 验证（非仅源码审查）。

---

## 一、测试执行总览（2026-08-22 实测，收口后）

```
dotnet build LPS.APS.sln   → 0 错误
dotnet test LPS.APS.Tests --filter "FullyQualifiedName~Unit"
                           → 104 通过 / 0 失败 / 0 跳过
```

- **单元测试 104/104 全绿**：含本轮新增 6 项（六块统一失败 ×2、P1-01 状态/有效期防御 ×2、Validator 发布链拒绝 ×2、发布聚合 ContentSnapshotJson 断言）与改写 4 项（正常装配六块真实重放、JSON 缺失/损坏来源迁移至 ContentSnapshotJson、失败不写缓存）
- **集成测试**：7 失败 / 1 通过 / 6 跳过——失败根因为 `RuleSetVersionRepository.AddAsync` 写 `[ContentSnapshotJson]` 列而 DB 尚无此列（**2号位 未执行《DDL变更申请_ContentSnapshotJson》**，红线 #6 外部阻塞），非 3号位 代码缺陷；2号位 落地 #15 变更申请后自动转绿
- 既有 1 失败（2号位 `FiniteCapacitySolver` DI）不属本轮范围

---

## 二、R01～R22 逐项映射与状态

| 编号 | 验收场景（实施包） | 测试文件 | 关键测试方法 | 状态 |
|---|---|---|---|---|
| R01 | 发布 RuleSetVersion，历史版本不可被覆盖 | `RuleSetVersionPublishTests` | `Publish_DraftVersion_StatusBecomesPublished` / `Publish_AlreadyPublishedVersion_ThrowsInvalidOperation` | ✅ 绿 |
| R02 | 发布 ParameterSetVersion，新 Run 可引用、旧 Run 不变 | `ParameterSetVersionPublishTests` | `Publish_NewV2_OldV1RecordUnchanged` / `Publish_AlreadyPublishedVersion_ThrowsInvalidOperation` + 发布聚合 `ContentSnapshotJson` 断言 | ✅ 绿 |
| R03 | Run 启动后发布新规则，当前 Run 仍用旧冻结版本 | `FrozenStrategySnapshotProviderTests` | `同一版本二次调用_命中缓存不重复查库` / `不同版本_互不污染各走仓储` / `缓存命中_反序列化重建独立实例_内容值相等FrozenAt刷新`（六块真实重放） | ✅ 绿 |
| R04 | Priority Segment 第一命中 | `DemandPriorityMatcherTests` | `R04_第一命中即止_命中第一个Segment后不再匹配后续` | ✅ 绿 |
| R05 | 同 Segment 多 Sort 字段顺序稳定 | `DemandPriorityMatcherTests` | `R05_多Sort稳定排序_依次应用排序字段` | ✅ 绿 |
| R06 | 不同 DemandType 同层竞争按 Segment 交错，不用全局 Score | `DemandPriorityMatcherTests` | `R06_不同DemandType交错_按Segment分组后组内排序` | ✅ 绿 |
| R07 | Demand Protection 触发，2号位能读取触发配置 | `StageDTests` | `R07_Protection触发配置_可读取` | ✅ 绿 |
| R08 | PI 默认排序（Issue/Create Time） | `StageDTests` | `R08_PI默认排序_可读取` | ✅ 绿 |
| R09 | Warehouse Availability 规则可解析 | `StageDTests` | `R09_WarehouseAvailability规则_可解析` | ✅ 绿 |
| R10 | Manual ETA > ERP ETA > DefaultLT 参数链可表达 | `StageDTests` + `EtaInvariantTests` | `R10_ETA链_优先级固定可表达` + 6 项 ETA 不变量 | ✅ 绿 |
| R11 | DefaultLT 逾期 Margin 可配置 | `StageDTests` | `R11_逾期Margin_可配置` | ✅ 绿 |
| R12 | Arrival Offset 按 Warehouse 级生效 | `StageDTests` | `R12_ArrivalOffset_按Warehouse级生效` | ✅ 绿 |
| R13 | Planning Yield 2号位可取正确版本值 | `StageDTests` + `FrozenStrategySnapshotProviderTests` | `R13_PlanningYield_可取正确版本值`（Procurement 子块含 PlanningYields） | ✅ 绿 |
| R14 | MIXED Solver Strategy 1号位能读取 | `FrozenStrategySnapshotProviderTests` | `正常装配六块_成功`：`snapshot.SolverStrategy.Mode == SolverStrategyMode.Backward`（真实重放，非 NotNull） | ✅ 绿 |
| R15 | On-time Target 取值正确 | 同上 | `snapshot.SolverStrategy.OnTimeTarget.TargetPercent == 85` / `Setup.DefaultSetupMinutes == 45` | ✅ 绿 |
| R16 | Candidate 60/90/180 Snapshot 中正确 | 同上 | `snapshot.CandidateGuardrail.NormalMs == 70_000 / SoftMs == 110_000 / LocalHardMs == 200_000` | ✅ 绿 |
| R17 | MaxImpactedOrders 仅 Warning 语义 | 同上 | `snapshot.CandidateGuardrail.WarnOnlyOnMaxImpacted == true`（含在 `CreateValidJson` 装配基准内）；六块统一失败语义覆盖 | ✅ 绿 |
| R18 | FULL ExpectedDomainKeysJson 多 Domain 合法 | `RunLifecycleServiceTests` | `Validate_FullSchedule_两Domain_通过` / `Validate_FullSchedule_重复DomainKey_抛异常` | ✅ 绿 |
| R19 | Candidate ExpectedDomainKeysJson 多于 1 个拒绝 | `RunLifecycleServiceTests` | `Validate_Reschedule_两Domain_抛异常` / `Validate_Reschedule_恰一Domain_通过` | ✅ 绿 |
| R20 | FAILED Run 人工恢复新建 Run | `RunLifecycleServiceTests` | `Recover_合法_新建RUNNING继承基线_旧记录不动` / `Recover_旧Run非FAILED_抛异常` | ✅ 绿 |
| R21 | Candidate 确认 Actor/Time/Version 可追溯 | `RunLifecycleServiceTests` | `Confirm_合法_仅记审计不写Activated` / `Activate_未确认_抛异常` / `Activate_合法_状态转ACTIVE` | ✅ 绿 |
| R22 | OA 不可用不阻断最小人工确认 | `RunLifecycleServiceTests` | `Confirm_*` 系列（仅审计落点，无 OA 依赖） | ✅ 绿 |

**合计：22/22 全绿（R01~R22）——P0-01b/P0-02a/P0-02b 收口后，R14~R17 由阻塞转绿。**
> 注：R14~R17 为单元测试层实测（Mock 版本数据）；集成测试层受 2号位 DDL 未落地阻塞（见 §一），转绿条件 = #15 变更申请执行。

---

## 三、二轮复审 P1-02 要求逐项核对（收口后全部满足）

| 要求 | 动作 | 证据 |
|---|---|---|
| 完整 Snapshot 具体值（六块） | ✅ 六块全部具体值断言 | `FrozenStrategySnapshotProviderTests`：DemandPriority.Segments[0].SegmentName / Lock.RemainingTimeThresholdHours / Supply.WarehousePriority / Procurement.PlanningYields[0].YieldPercent / SolverStrategy.Mode+TargetPercent+Setup / CandidateGuardrail.NormalMs+SoftMs+LocalHardMs |
| Run 中规则不漂移 | ✅ 版本隔离 + 缓存不污染 | `同一版本二次调用` / `不同版本_互不污染` |
| PlanningYield | ✅ | `StageDTests.R13` |
| SolverStrategy / CandidateGuardrail | ✅ 真实来源收口 | Provider 从 `ContentSnapshotJson.SolverStrategy`/`.CandidateGuardrail` 子块反序列化（契约 §6.10.5），替代空对象；发布链 `ValidateParameterSetVersionForPublishAsync` 接线两 Validator |
| CTP/Impact 永不激活 | ✅ | `Activate_来源Run为INSERT_ORDER_WHATIF_抛异常` |
| 确认后才激活 | ✅ | `Activate_未确认_抛异常` |
| Base ACTIVE 原子替换 | ✅ | `Activate_同域已有ACTIVE_原子替换` |
| FULL 重复 Domain 拒绝 | ✅ | `Validate_FullSchedule_重复DomainKey_抛异常` |
| FAILED 新 Run | ✅ | `Recover_*` 4 项 |

---

## 四、本轮收口变更（#16~#19，对应第三轮提报清单 11~13 关闭）

| 清单项 | v1.0 状态 | v2.0 状态 | 落点 |
|---|---|---|---|
| 11 DDL/内容快照落点一致 | ❌ 阻塞（待裁决） | ✅ 关闭 | 《DDL变更申请_ContentSnapshotJson_v1.0》提交 2号位（红线 #6）；0号位 批准方案 A；实体/Repository SQL 对齐（`ContentSnapshotJson` + `EffectiveFrom/To/ApprovedAt/By` 双写） |
| 12 六块按 VersionId 重放具体值 | ❌ 阻塞 | ✅ 关闭 | Provider 六块统一从 `ContentSnapshotJson` 子块反序列化；SolverStrategy/CandidateGuardrail 真实来源；R14~R17 具体值断言 |
| 13 必填损坏/缺失失败，Solver/Candidate 不再空对象 | 🟡 部分 | ✅ 关闭 | 六块统一缺失/损坏一律装载失败（新增 2 用例）；显式 `"{}"` 为合法默认 |

### 发布链与装载链闭环（契约 §6.10.5）

- **发布阶段**：`PublishRuleSetVersionAsync` 聚合 `DemandPriority` 子块 → `ContentSnapshotJson`；`PublishParameterSetVersionAsync` 聚合五子块（Lock/Supply/Procurement/SolverStrategy/CandidateGuardrail）→ `ContentSnapshotJson`；发布前 `ValidateParameterSetVersionForPublishAsync` 接线 `SolverStrategyValidator`/`CandidateGuardrailValidator`（非法内容拒绝发布，新增 2 用例）
- **装载阶段**：`GetFrozenStrategySnapshotAsync` 按 `StrategyProfileVersionId` → 两版本 `ContentSnapshotJson` → 六块反序列化；**P1-01 防御**：非 PUBLISHED / 未生效 / 已失效版本一律装载失败（新增 2 用例）

---

## 五、跨号位依赖（2号位）

1. **执行《DDL变更申请_ContentSnapshotJson》**（RuleSetVersion + ParameterSetVersion 各增 `ContentSnapshotJson NVARCHAR(MAX) NULL`）→ 集成测试 7 失败自动转绿
2. **既有 1 失败**（`FiniteCapacitySolver` DI）归属 2号位（《P0整改复核提报》§六.3）
3. **6 个跳过（环境）**：APS_Auth 库 + `ExpectedDomainKeysJson` 列迁移后自动转绿

---

## 附：本轮（#16~#19）3号位 代码变更清单

| 文件 | 变更 |
|---|---|
| `LPS.APS.Core\Entities\Aps\RuleSetVersion.cs` | +`ContentSnapshotJson`；-`UpdatedAt/UpdatedBy`（冻结 DDL 无列）；`DemandPriorityJson` 注释为发布装配中间态 |
| `LPS.APS.Core\Entities\Aps\ParameterSetVersion.cs` | +`ContentSnapshotJson`/`SolverStrategyJson`/`CandidateGuardrailJson`；-`UpdatedAt/UpdatedBy` |
| `LPS.APS.Engine\Repositories\Governance\RuleSetVersionRepository.cs` | INSERT/UPDATE 对齐方案 A（ContentSnapshotJson + 正式字段双写；移除非冻结列） |
| `LPS.APS.Engine\Repositories\Governance\ParameterSetVersionRepository.cs` | 同上 |
| `LPS.APS.Web\Controllers\GovernanceController.cs` | 移除 4 处 `UpdatedAt` 死写入（无 DDL 列、无消费方） |
| `LPS.APS.Application\Services\FrozenStrategySnapshotProvider.cs` | 六块统一从 ContentSnapshotJson 反序列化（真实来源）；+`EnsureLoadable`（P1-01 防御）；删除主题列专用反序列化方法 |
| `LPS.APS.Application\Services\GovernanceVersionService.cs` | +两 Validator 接线（ValidateSolverStrategyJson/ValidateCandidateGuardrailJson）；发布聚合 `BuildRuleSetContentSnapshot`/`BuildParameterSetContentSnapshot`；+两 Validator 字段 |
| `LPS.APS.Tests\Unit\FrozenStrategySnapshotProviderTests.cs` | 装配辅助迁移 ContentSnapshotJson；正常装配六块真实值断言（R14~R17）；+六块统一失败 ×2；+P1-01 防御 ×2；JSON 缺失/损坏来源迁移 |
| `LPS.APS.Tests\Unit\ParameterSetVersionPublishTests.cs` | `CreateValidJson` 五块化；+发布聚合 ContentSnapshotJson 断言；+Validator 拒绝路径 ×2 |
